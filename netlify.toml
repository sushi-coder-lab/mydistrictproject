[build]
  publish = "frontend"
  command = "echo 'No build command needed for plain HTML/CSS/JS frontend'"

[[redirects]]
  from = "/api/*"
  to = "https://dantewada-backend.onrender.com/api/:splat"
  status = 200
